class Solution {
    public int reverseExponentiation(int n) {
        int copy = n;
        int m = 0;
        while(n>0){
            int digit = n%10;
            m = (m*10)+digit;
            n=n/10;
        }
        int a = (int)Math.pow(copy,m);
        return a;
        
    }
}
